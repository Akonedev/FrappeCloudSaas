# Press SaaS Platform - Environment Configuration Example
# Copy this file to .env and update with your actual values

# =============================================================================
# FRAPPE / ERPNEXT CONFIGURATION
# =============================================================================

# Frappe/ERPNext version (v15 or v16)
ERPNEXT_VERSION=v16

# Custom Docker image for Frappe
CUSTOM_IMAGE=frappe/erpnext
CUSTOM_TAG=v16

# Container restart policy
RESTART_POLICY=unless-stopped

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL 16)
# =============================================================================

# PostgreSQL root password (REQUIRED - used by db service)
DB_PASSWORD=fcs_press_secure_password_2025

# PostgreSQL connection details (used by configurator)
DB_HOST=db
DB_PORT=5432

# PostgreSQL user and database (for Press Manager if used)
POSTGRES_USER=postgres
POSTGRES_DB=postgres

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================

# Redis endpoints (configured in compose.redis.yaml)
REDIS_CACHE=redis-cache:6379
REDIS_QUEUE=redis-queue:6379

# =============================================================================
# NETWORK & PORTS CONFIGURATION
# =============================================================================

# Frontend Nginx port (main entry point)
HTTP_PUBLISH_PORT=48580

# PostgreSQL external port
POSTGRES_PORT=48532

# Redis Cache external port
REDIS_CACHE_PORT=48510

# Redis Queue external port
REDIS_QUEUE_PORT=48511

# =============================================================================
# MINIO CONFIGURATION (Optional - for S3-compatible storage)
# =============================================================================

# MinIO credentials (if using S3 storage)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123

# =============================================================================
# KEYCLOAK SSO CONFIGURATION (Optional - for SSO authentication)
# =============================================================================

# Keycloak admin credentials
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin123

# =============================================================================
# PRESS MANAGER API CONFIGURATION (Optional - for Press Manager backend)
# =============================================================================

# Press Manager host and port
PRESS_MANAGER_HOST=0.0.0.0
PRESS_MANAGER_PORT=48550

# =============================================================================
# NOTES
# =============================================================================
#
# Required variables for basic deployment:
# - DB_PASSWORD
# - ERPNEXT_VERSION
# - HTTP_PUBLISH_PORT
#
# Port ranges used: 48510-49800
# - 48510: Redis Cache
# - 48511: Redis Queue
# - 48532: PostgreSQL
# - 48550: Press Manager API (optional)
# - 48580: Frontend Nginx (main entry point)
#
# Network: All services use 'fcs-press-network' (external network)
#
# For complete deployment guide, see:
# - docs/DEPLOYMENT.md
# - specs/001-press-saas-platform/quickstart.md
#
